<div class="container">
    <div class="row d-flex justify-content-center">
        <div class="col-4">
            <h5 class="h1">Editar nota</h5>
            <form method="post">
                <input type="hidden" name="_method" value="put">
                <div class="form-group">
                    <label for="new_note_title">Título</label>
                    <input type="text" name="title" id="new_note_title" class="form-control" value="{{ note.title }}">
                </div>
                <div class="form-group mb-2">
                    <label for="new_note_body">Texto</label>
                    <textarea name="body" id="new_note_body" class="form-control">{{ note.body }}</textarea>
                </div>
                <div class="d-grid gap-2">
                    <button type="submit" class="btn btn-primary">Guardar nota</button>
                </div>
            </form>
        </div>
    </div>
</div>